// Re-export from shared package — this file exists for backward compatibility
export {
  getEffectiveContextLimit,
  getModelContextLimit,
  MODEL_CONTEXT_LIMITS,
  DEFAULT_CONTEXT_LIMIT,
  CONTEXT_RATIO,
} from '@extension/shared';
